/** Generated by BaggageBuffersCompiler */
package brownsys.tracingplane.opentracing;

import edu.brown.cs.systems.tracingplane.baggage_buffers.BaggageBuffers;
import edu.brown.cs.systems.tracingplane.baggage_buffers.Registrations;
import edu.brown.cs.systems.tracingplane.baggage_buffers.api.Bag;
import edu.brown.cs.systems.tracingplane.baggage_buffers.api.BaggageHandler;
import edu.brown.cs.systems.tracingplane.baggage_buffers.api.Brancher;
import edu.brown.cs.systems.tracingplane.baggage_buffers.api.Joiner;
import edu.brown.cs.systems.tracingplane.baggage_buffers.api.Parser;
import edu.brown.cs.systems.tracingplane.baggage_buffers.api.Serializer;
import edu.brown.cs.systems.tracingplane.baggage_buffers.impl.BBUtils;
import edu.brown.cs.systems.tracingplane.baggage_buffers.impl.Branchers;
import edu.brown.cs.systems.tracingplane.baggage_buffers.impl.Joiners;
import edu.brown.cs.systems.tracingplane.baggage_buffers.impl.Parsers;
import edu.brown.cs.systems.tracingplane.baggage_buffers.impl.Serializers;
import edu.brown.cs.systems.tracingplane.baggage_layer.BagKey;
import edu.brown.cs.systems.tracingplane.baggage_layer.protocol.BaggageReader;
import edu.brown.cs.systems.tracingplane.baggage_layer.protocol.BaggageWriter;
import edu.brown.cs.systems.tracingplane.transit_layer.Baggage;
import java.util.Set;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;


public class WrappedContexts implements Bag {

    private static final Logger _log = LoggerFactory.getLogger(WrappedContexts.class);

    public Set<java.nio.ByteBuffer> contexts = null;

    public boolean _overflow = false;

    /**
    * <p>
    * Get the {@link WrappedContexts} set in the {@link Baggage} carried by the current thread. If no baggage is being
    * carried by the current thread, or if there is no WrappedContexts in it, then this method returns {@code null}.
    * </p>
    *
    * <p>
    * To get WrappedContexts from a specific Baggage instance, use {@link #getFrom(Baggage)}.
    * </p>
    *
    * @return the WrappedContexts being carried in the {@link Baggage} of the current thread, or {@code null}
    *         if none is being carried. The returned instance maybe be modified and modifications will be reflected in
    *         the baggage.
    */
    public static WrappedContexts get() {
        Bag bag = BaggageBuffers.get(Handler.registration());
        if (bag instanceof WrappedContexts) {
            return (WrappedContexts) bag;
        } else {
            return null;
        }
    }

    /**
    * <p>
    * Get the {@link WrappedContexts} set in {@code baggage}. If {@code baggage} has no WrappedContexts set then
    * this method returns null.
    * </p>
    *
    * <p>
    * This method does <b>not</b> affect the Baggage being carried by the current thread.  To get WrappedContexts
    * from the current thread's Baggage, use {@link #get()}.
    * </p>
    *
    * @param baggage A baggage instance to get the {@link WrappedContexts} from
    * @return the {@link WrappedContexts} instance being carried in {@code baggage}, or {@code null} if none is being carried.
    *         The returned instance can be modified, and modifications will be reflected in the baggage.
    */
    public static WrappedContexts getFrom(Baggage baggage) {
        Bag bag = BaggageBuffers.get(baggage, Handler.registration());
        if (bag instanceof WrappedContexts) {
            return (WrappedContexts) bag;
        } else if (bag != null) {
            Handler.checkRegistration();
        }
        return null;
    }

    /**
    * <p>
    * Update the {@link WrappedContexts} set in the current thread's baggage. This method will overwrite any existing
    * WrappedContexts set in the current thread's baggage.
    * </p>
    *
    * <p>
    * To set the {@link WrappedContexts} in a specific {@link Baggage} instance, use
    * {@link #setIn(Baggage, WrappedContexts)}
    * </p>
    *
    * @param wrappedContexts the new {@link WrappedContexts} to set in the current thread's {@link Baggage}. If {@code null}
    *            then any existing mappings will be removed.
    */
    public static void set(WrappedContexts wrappedContexts) {
        BaggageBuffers.set(Handler.registration(), wrappedContexts);
    }

    /**
    * <p>
    * Update the {@link WrappedContexts} set in {@code baggage}. This method will overwrite any existing
    * WrappedContexts set in {@code baggage}.
    * </p>
    *
    * <p>
    * This method does <b>not</b> affect the {@link Baggage} being carried by the current thread. To set the
    * {@link WrappedContexts} for the current thread, use {@link #set(WrappedContexts)}
    * </p>
    *
    * @param baggage A baggage instance to set the {@link WrappedContexts} in
    * @param wrappedContexts the new WrappedContexts to set in {@code baggage}. If {@code null}, it will remove any
    *            mapping present.
    * @return a possibly new {@link Baggage} instance that contains all previous mappings plus the new mapping.
    */
    public static Baggage setIn(Baggage baggage, WrappedContexts wrappedContexts) {
        return BaggageBuffers.set(baggage, Handler.registration(), wrappedContexts);
    }

    @Override
    public BaggageHandler<?> handler() {
        return Handler.instance;
    }

    @Override
    public String toString() {
        StringBuilder b = new StringBuilder();
        b.append("WrappedContexts{\n");
            b.append(this.contexts == null ? "" : BBUtils.indent(String.format("contexts = %s\n", BBUtils.toString(this.contexts))));
            b.append("}");
        return b.toString();
    }

    public static class Handler implements BaggageHandler<WrappedContexts> {

        public static final Handler instance = new Handler();
        private static BagKey registration = null;

        static synchronized BagKey checkRegistration() {
            registration = Registrations.lookup(instance);
            if (registration == null) {
                _log.error("WrappedContexts MUST be registered to a key before it can be propagated.  " +
                "There is currently no registration for WrappedContexts and it will not be propagated. " +
                "To register a bag set the baggage-buffers.bags property in your application.conf " +
                "or with -Dbaggage-buffers.bags flag (eg, for key 10, -Dbaggage-buffers.bags.10=" + WrappedContexts.class.getName());
            }
            return registration;
        }

        static BagKey registration() {
            return registration == null ? checkRegistration() : registration;
        }

        private Handler(){}

        private static final BagKey _contextsKey = BagKey.indexed(0);

        private static final Parser<Set<java.nio.ByteBuffer>> _contextsParser = Parsers.setParser(Parsers.bytesParser());
        private static final Serializer<Set<java.nio.ByteBuffer>> _contextsSerializer = Serializers.setSerializer(Serializers.bytesSerializer());
        private static final Brancher<Set<java.nio.ByteBuffer>> _contextsBrancher = Branchers.<java.nio.ByteBuffer>set();
        private static final Joiner<Set<java.nio.ByteBuffer>> _contextsJoiner = Joiners.<java.nio.ByteBuffer>setUnion();

        @Override
        public boolean isInstance(Bag bag) {
            return bag == null || bag instanceof WrappedContexts;
        }

        @Override
        public WrappedContexts parse(BaggageReader reader) {
            WrappedContexts instance = new WrappedContexts();

            if (reader.enter(_contextsKey)) {
                instance.contexts = _contextsParser.parse(reader);
                reader.exit();
            }
            instance._overflow = reader.didOverflow();

            return instance;
        }

        @Override
        public void serialize(BaggageWriter writer, WrappedContexts instance) {
            if (instance == null) {
                return;
            }

            writer.didOverflowHere(instance._overflow);

            if (instance.contexts != null) {
                writer.enter(_contextsKey);
                _contextsSerializer.serialize(writer, instance.contexts);
                writer.exit();
            }
        }

        @Override
        public WrappedContexts branch(WrappedContexts instance) {
            if (instance == null) {
                return null;
            }

            WrappedContexts newInstance = new WrappedContexts();
            newInstance.contexts = _contextsBrancher.branch(instance.contexts);
            return newInstance;
        }

        @Override
        public WrappedContexts join(WrappedContexts left, WrappedContexts right) {
            if (left == null) {
                return right;
            } else if (right == null) {
                return left;
            } else {
                left.contexts = _contextsJoiner.join(left.contexts, right.contexts);
                return left;
            }
        }
    }
}